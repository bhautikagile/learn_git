import { styled } from 'styled-components';

export const LoaderWrapper = styled.h5`
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 9999;
  transition:
    all 0.3s,
    color 0.3s;
`;